# 缩略图滚动崩溃修复与性能优化方案

## 问题诊断

基于用户反馈和代码审计，发现以下关键问题：

### 🔴 P0 - 致命崩溃问题

#### 1. CreateBitmapOnUIThreadAsync 中的资源管理缺陷
**位置**: `ImageFileInfo.cs:78-120`

**问题**:
```csharp
var registration = cancellationToken.Register(() => tcs.TrySetCanceled());

dispatcherQueue.TryEnqueue(DispatcherQueuePriority.Low, () =>
{
    try
    {
        // ...
    }
    finally
    {
        registration.Dispose();  // ❌ 问题：registration 在外部作用域
    }
});
```

**缺陷**:
- `registration` 变量在闭包外部定义，但在 UI 线程回调中释放
- 多个并发加载任务会导致 `registration` 被覆盖
- 可能导致内存泄漏或访问已释放资源

#### 2. EnsureThumbnailAsync 中的竞态条件
**位置**: `ImageFileInfo.cs:134-158`

**问题**:
- `_thumbnailLoadCts` 和 `_loadVersion` 的操作不是原子的
- 快速滚动时多个 `EnsureThumbnailAsync` 可能同时执行
- 没有使用锁保护共享变量

#### 3. 已加载图片二次加载的问题
**位置**: `ImageFileInfo.cs:134-158`

**问题**:
```csharp
public async Task EnsureThumbnailAsync(ThumbnailSize size)
{
    _thumbnailLoadCts?.Cancel();  // ❌ 即使 Thumbnail != null 也取消
    _thumbnailLoadCts = new CancellationTokenSource();
    // ...
}
```

**缺陷**:
- 滚动回已加载过的图片时，即使 `Thumbnail != null`，也会取消任务
- 导致已加载的缩略图被取消加载
- 二次返回浏览时需要重新加载

---

### 🟡 P1 - 性能优化问题

#### 4. 缺少缩略图尺寸跟踪
**问题**:
- 没有记录当前已加载的缩略图尺寸
- 切换大小时会清除所有已加载的缩略图，即使尺寸相同

#### 5. 缺少滚动事件监听
**问题**:
- 没有利用 ScrollView 的滚动事件
- 无法在快速滚动时暂停非关键任务
- 无法根据滚动速度调整加载优先级

---

## 优化方案

### [ ] 任务 1: 修复 CreateBitmapOnUIThreadAsync 资源管理
- **Priority**: P0
- **Depends On**: None
- **Description**:
  - 将 `registration` 的声明移到 TryEnqueue 回调内部
  - 或者使用正确的闭包捕获方式
  - 确保每个任务的 registration 独立管理
- **Success Criteria**:
  - 多个并发加载任务不会互相干扰
  - 资源正确释放，无内存泄漏
- **Test Requirements**:
  - `programmatic` TR-1.1: 快速滚动 100 次不崩溃
  - `human-judgement` TR-1.2: 内存使用稳定，无明显增长
- **Notes**: 这是导致崩溃的核心原因

### [ ] 任务 2: 修复 EnsureThumbnailAsync 竞态条件
- **Priority**: P0
- **Depends On**: None
- **Description**:
  - 添加 `object _thumbnailLoadLock = new()` 锁对象
  - 使用锁保护 `_thumbnailLoadCts` 和 `_loadVersion` 的操作
  - 确保原子性
- **Success Criteria**:
  - 多个线程同时调用 EnsureThumbnailAsync 不会导致崩溃
- **Test Requirements**:
  - `programmatic` TR-2.1: 并发调用 100 次 EnsureThumbnailAsync 无异常
  - `human-judgement` TR-2.2: 快速来回拖拽滚动条不崩溃

### [ ] 任务 3: 添加缩略图已加载检查
- **Priority**: P0
- **Depends On**: None
- **Description**:
  - 在 `EnsureThumbnailAsync` 开始时检查 `Thumbnail != null`
  - 如果已加载且尺寸匹配，直接返回
  - 添加 `ThumbnailSize? _loadedThumbnailSize` 字段跟踪已加载尺寸
- **Success Criteria**:
  - 滚动回已加载图片时不再重新加载
  - 二次浏览瞬间显示
- **Test Requirements**:
  - `programmatic` TR-3.1: 已加载图片二次返回时 EnsureThumbnailAsync 立即返回
  - `human-judgement` TR-3.2: 快速来回滚动时已加载图片瞬间显示

### [ ] 任务 4: 优化缩略图尺寸切换
- **Priority**: P1
- **Depends On**: Task 3
- **Description**:
  - 利用 `_loadedThumbnailSize` 字段
  - 在 `ViewModel_ThumbnailSizeChanged` 中只清除尺寸不匹配的缩略图
  - 避免不必要的重新加载
- **Success Criteria**:
  - 切换到已加载过的尺寸时，已加载图片保持显示
- **Test Requirements**:
  - `programmatic` TR-4.1: 尺寸切换时只清除尺寸不匹配的缩略图
  - `human-judgement` TR-4.2: 尺寸切换更流畅

### [ ] 任务 5: 添加滚动速度感知（可选）
- **Priority**: P2
- **Depends On**: None
- **Description**:
  - 监听 ScrollView 的 ViewChanged 事件
  - 计算滚动速度
  - 快速滚动时降低加载优先级或暂停
  - 滚动停止后恢复加载
- **Success Criteria**:
  - 快速滚动时更流畅，UI 响应更快
- **Test Requirements**:
  - `human-judgement` TR-5.1: 快速滚动时 UI 响应无延迟
  - `human-judgement` TR-5.2: 滚动停止后缩略图正常加载

---

## 关键技术实现细节

### Task 1 实现示例
```csharp
private static async Task<BitmapImage> CreateBitmapOnUIThreadAsync(IRandomAccessStream stream, CancellationToken cancellationToken)
{
    BitmapImage? bitmap = null;

    var dispatcherQueue = App.MainWindow.DispatcherQueue;
    if (dispatcherQueue == null)
    {
        bitmap = new BitmapImage();
        bitmap.SetSource(stream);
        return bitmap;
    }

    var tcs = new TaskCompletionSource<bool>();

    dispatcherQueue.TryEnqueue(DispatcherQueuePriority.Low, () =>
    {
        // ✅ 修复：registration 在回调内部声明
        using var registration = cancellationToken.Register(() => tcs.TrySetCanceled());
        
        try
        {
            if (cancellationToken.IsCancellationRequested)
            {
                tcs.TrySetCanceled();
                return;
            }

            bitmap = new BitmapImage();
            bitmap.SetSource(stream);
            tcs.SetResult(true);
        }
        catch (Exception ex)
        {
            tcs.SetException(ex);
        }
    });

    await tcs.Task;
    return bitmap!;
}
```

### Task 2 + Task 3 实现示例
```csharp
public class ImageFileInfo : INotifyPropertyChanged
{
    // ... 现有字段 ...
    private readonly object _thumbnailLoadLock = new();
    private ThumbnailSize? _loadedThumbnailSize;

    public async Task EnsureThumbnailAsync(ThumbnailSize size)
    {
        // ✅ 修复：先检查是否已加载
        lock (_thumbnailLoadLock)
        {
            if (Thumbnail != null && _loadedThumbnailSize == size)
                return;
        }

        CancellationTokenSource localCts;
        int localVersion;

        lock (_thumbnailLoadLock)
        {
            _thumbnailLoadCts?.Cancel();
            _thumbnailLoadCts = new CancellationTokenSource();
            localCts = _thumbnailLoadCts;
            localVersion = ++_loadVersion;
        }

        var cancellationToken = localCts.Token;

        try
        {
            var result = await GetThumbnailAsync(size, cancellationToken);

            lock (_thumbnailLoadLock)
            {
                if (localVersion != _loadVersion || cancellationToken.IsCancellationRequested)
                    return;

                Thumbnail = result;
                _loadedThumbnailSize = size;
            }
        }
        catch (OperationCanceledException)
        {
        }
        catch (Exception ex)
        {
            System.Diagnostics.Debug.WriteLine($"EnsureThumbnailAsync error: {ex}");
        }
    }

    public void ClearThumbnail()
    {
        lock (_thumbnailLoadLock)
        {
            _thumbnailLoadCts?.Cancel();
            _loadVersion++;
            Thumbnail = null;
            _loadedThumbnailSize = null;
        }
    }
}
```

---

## 实施优先级

### 第一阶段（必须先做）
1. Task 1 - 修复 CreateBitmapOnUIThreadAsync 资源管理
2. Task 2 - 修复 EnsureThumbnailAsync 竞态条件
3. Task 3 - 添加缩略图已加载检查

### 第二阶段（优化）
4. Task 4 - 优化缩略图尺寸切换

### 第三阶段（增强体验）
5. Task 5 - 添加滚动速度感知（可选）

---

## 预期效果

✅ **快速滚动**：不再崩溃，流畅滚动  
✅ **二次浏览**：已加载图片瞬间显示，无需重新加载  
✅ **加载速度**：避免重复加载，性能提升  
✅ **内存使用**：更稳定，无内存泄漏  
✅ **尺寸切换**：更流畅，避免不必要的重新加载

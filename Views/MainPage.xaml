<Page
    x:Class="PhotoView.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:PhotoView.Models"
    xmlns:common="using:PhotoView.Common"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="ItemsRepeater_ItemTemplate" x:DataType="models:ImageFileInfo">
            <StackPanel Width="{x:Bind Width}" Height="{x:Bind Height}">
                <Button ContextRequested="imageButton_ContextRequested" x:Name="imageButton" AutomationProperties.Name="mountain" Padding="0">
                    <Image x:Name="ItemImage" Source="{x:Bind ImageFile.Path}" Stretch="Uniform"/>
                </Button>
            </StackPanel>
        </DataTemplate>
        <common:ActivityWaterfallFlowLayout x:Key="ItemsRepeater_ItemLayout" ColumnSpacing="12"
                          RowSpacing="12" MinItemSize="300, 200"/>
        <CommandBarFlyout Placement="Right" x:Name="CommandBarFlyout1">
            <AppBarButton Label="Share" Icon="Share" ToolTipService.ToolTip="Share" Click="OnElementClicked" />
            <AppBarButton Label="Save" Icon="Save" ToolTipService.ToolTip="Save" Click="OnElementClicked" />
            <AppBarButton Label="Delete" Icon="Delete" ToolTipService.ToolTip="Delete" Click="OnElementClicked" />
            <CommandBarFlyout.SecondaryCommands>
                <AppBarButton x:Name="ResizeButton1" Label="Resize" Click="ResizeButton1_Click" />
                <AppBarButton x:Name="MoveButton1" Label="Move" Click="OnElementClicked" />
            </CommandBarFlyout.SecondaryCommands>
        </CommandBarFlyout>

    </Page.Resources>
    <StackPanel>
        <RelativePanel>
            <Button x:Name="BarButtom1" Content="Edit Options 1" Height="50" RelativePanel.AlignRightWithPanel="True">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Share">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE72D;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Copy" Icon="Copy"/>
                        <MenuFlyoutItem Text="Delete" Icon="Delete"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Rename"/>
                        <MenuFlyoutItem Text="Select"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <Button x:Name="BarButtom2" Content="Edit Options 2" Height="50" RelativePanel.AlignRightWithPanel="True" RelativePanel.LeftOf="BarButtom1">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Share">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE72D;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Copy" Icon="Copy"/>
                        <MenuFlyoutItem Text="Delete" Icon="Delete"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Rename"/>
                        <MenuFlyoutItem Text="Select"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <!--<Rectangle x:Name="Rectangle1" Fill="Red" Height="50" Width="50"/>
            <Rectangle x:Name="Rectangle2" Fill="Blue" Height="50" Width="50" RelativePanel.RightOf="Rectangle1" Margin="8,0,0,0"/>
            <Rectangle x:Name="Rectangle3" Fill="Green" Height="50" Width="50" RelativePanel.AlignRightWithPanel="True"/>
            <Rectangle x:Name="Rectangle4" Fill="Yellow" Height="50" Width="50" RelativePanel.Below="Rectangle3" RelativePanel.AlignHorizontalCenterWith="Rectangle3" Margin="0,8,0,0"/>-->
        </RelativePanel>
        <ScrollViewer x:Name="scrollViewer"
                IsVerticalScrollChainingEnabled="False"
                Padding="0,0,16,0">
            <ItemsRepeater x:Name="ImageItemsRepeater" ItemsSource="{x:Bind ViewModel.Images}" Layout="{StaticResource ItemsRepeater_ItemLayout}" ItemTemplate="{StaticResource ItemsRepeater_ItemTemplate}"/>
        </ScrollViewer>
    </StackPanel>
</Page>

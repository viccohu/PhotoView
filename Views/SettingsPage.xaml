<Page
    x:Class="PhotoView.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Page.Resources>
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>
        
        <Style
            x:Key="SettingsSectionHeaderTextBlockStyle"
            BasedOn="{StaticResource BodyStrongTextBlockStyle}"
            TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>
    
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock
            x:Name="PageHeader"
            MaxWidth="1064"
            Margin="36,24,36,0"
            AutomationProperties.HeadingLevel="Level1"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Settings" />
        
        <ScrollView
            Grid.Row="1"
            Padding="36,0,36,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Border>
                <StackPanel MaxWidth="1064" Spacing="{StaticResource SettingsCardSpacing}">
                    <StackPanel.ChildrenTransitions>
                        <RepositionThemeTransition />
                    </StackPanel.ChildrenTransitions>
                    
                    <!-- Appearance & behavior -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Appearance &amp; behavior" />
                    
                    <toolkit:SettingsCard Description="Select which app theme to display" Header="App theme">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE790;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox
                            x:Name="themeMode"
                            AutomationProperties.AutomationId="themeModeComboBox"
                            SelectionChanged="themeMode_SelectionChanged">
                            <ComboBoxItem Content="Light" Tag="Light" />
                            <ComboBoxItem Content="Dark" Tag="Dark" />
                            <ComboBoxItem Content="Use system setting" Tag="Default" />
                        </ComboBox>
                    </toolkit:SettingsCard>
                    
                    <toolkit:SettingsCard Header="Navigation style">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xF594;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="navigationLocation" SelectionChanged="navigationLocation_SelectionChanged">
                            <ComboBoxItem Content="Top" />
                            <ComboBoxItem Content="Left" />
                        </ComboBox>
                    </toolkit:SettingsCard>
                    
                    <!-- Performance -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Performance" />
                    
                    <toolkit:SettingsCard Description="Adjust thumbnail loading performance" Header="Performance mode">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE9D9;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="PerformanceModeComboBox" SelectionChanged="PerformanceModeComboBox_SelectionChanged">
                            <ComboBoxItem Content="Smart (auto-adjust based on CPU)" Tag="Smart" />
                            <ComboBoxItem Content="Stable (4 concurrent)" Tag="Stable" />
                        </ComboBox>
                    </toolkit:SettingsCard>
                    
                    <toolkit:SettingsCard Description="Number of images to load per batch" Header="Batch size">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE8FD;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="BatchSizeComboBox" SelectionChanged="BatchSizeComboBox_SelectionChanged">
                            <ComboBoxItem Content="10" Tag="10" />
                            <ComboBoxItem Content="30" Tag="30" />
                            <ComboBoxItem Content="50" Tag="50" />
                            <ComboBoxItem Content="100" Tag="100" />
                        </ComboBox>
                    </toolkit:SettingsCard>
                    
                    <!-- Display -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Display" />
                    
                    <toolkit:SettingsCard Description="Remember the last opened folder" Header="Remember last folder">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE838;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="RememberLastFolderToggleSwitch" Toggled="RememberLastFolderToggleSwitch_Toggled" />
                    </toolkit:SettingsCard>
                    
                    <toolkit:SettingsCard Description="Set the default thumbnail display size" Header="Default thumbnail size">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE7B8;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="ThumbnailSizeComboBox" SelectionChanged="ThumbnailSizeComboBox_SelectionChanged">
                            <ComboBoxItem Content="Small" Tag="Small" />
                            <ComboBoxItem Content="Medium" Tag="Medium" />
                            <ComboBoxItem Content="Large" Tag="Large" />
                        </ComboBox>
                    </toolkit:SettingsCard>
                    
                    <!-- File Operations -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="File Operations" />
                    
                    <toolkit:SettingsCard Description="Move deleted files to recycle bin (not available for removable drives)" Header="Delete to recycle bin">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE74D;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ToggleSwitch x:Name="DeleteToRecycleBinToggleSwitch" Toggled="DeleteToRecycleBinToggleSwitch_Toggled" />
                    </toolkit:SettingsCard>
                    
                    <!-- About -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="About" />
                    
                    <toolkit:SettingsExpander Margin="0,0,0,36" Header="PhotoView">
                        <toolkit:SettingsExpander.HeaderIcon>
                            <BitmapIcon ShowAsMonochrome="False" UriSource="/Assets/WindowIcon.ico" />
                        </toolkit:SettingsExpander.HeaderIcon>
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}" />
                        <toolkit:SettingsExpander.Items>
                            <toolkit:SettingsCard Header="Privacy Statement">
                                <toolkit:SettingsCard.ActionIcon>
                                    <FontIcon Glyph="&#xE8A7;" />
                                </toolkit:SettingsCard.ActionIcon>
                            </toolkit:SettingsCard>
                        </toolkit:SettingsExpander.Items>
                    </toolkit:SettingsExpander>
                </StackPanel>
            </Border>
        </ScrollView>
    </Grid>
</Page>

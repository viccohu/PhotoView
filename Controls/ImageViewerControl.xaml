<UserControl
    x:Class="PhotoView.Controls.ImageViewerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid
        x:Name="BackgroundOverlay"
        Background="#80000000"
        Opacity="0"
        Tapped="BackgroundOverlay_Tapped">
        
        <Grid
            x:Name="AnimationContainer"
            Opacity="0">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer
                x:Name="ImageScrollViewer"
                Grid.Column="0"
                Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                VerticalScrollMode="Disabled"
                ZoomMode="Enabled"
                Tapped="ImageScrollViewer_Tapped"
                PointerWheelChanged="ImageScrollViewer_PointerWheelChanged">
                <Grid
                    x:Name="ImageContainer"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <Image
                        x:Name="mainImage"
                        Stretch="Uniform"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        DoubleTapped="mainImage_DoubleTapped"/>
                </Grid>
            </ScrollViewer>

            <Border
                x:Name="InfoPanel"
                Grid.Column="1"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                Padding="24"
                Tapped="InfoPanel_Tapped">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="16">
                        <Grid>
                            <TextBlock Text="信息" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,48,0"/>
                            <Button
                                x:Name="CloseButton"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Click="CloseButton_Click">
                                <FontIcon Glyph="&#xE711;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                            </Button>
                        </Grid>

                        <StackPanel Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8B9;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <TextBlock x:Name="ImageNameTextBox" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE787;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <DatePicker x:Name="CaptureDatePicker" Header="拍摄日期" IsEnabled="False"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8C5;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <TimePicker x:Name="CaptureTimePicker" Header="拍摄时间" IsEnabled="False"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8A9;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <TextBlock Text="大小信息" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="16" Margin="24,0,0,0">
                                <TextBlock x:Name="ResolutionTextBlock" Style="{ThemeResource BodyTextBlockStyle}"/>
                                <TextBlock x:Name="FileSizeTextBlock" Style="{ThemeResource BodyTextBlockStyle}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE722;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <TextBlock Text="设备信息" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            </StackPanel>
                            <StackPanel x:Name="DeviceInfoPanel" Orientation="Horizontal" Spacing="8" Margin="24,0,0,0"
                                          Visibility="Collapsed">
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8B7;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                <TextBlock Text="文件路径" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                            </StackPanel>
                            <StackPanel x:Name="FilePathPanel" Spacing="8" Margin="24,0,0,0">
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
